#include<bits/stdc++.h>
using namespace std;
int main()
{ //  Reverse
    int n = 5;
    int ar[5];
    string st;
    sort(ar, ar + n);
    reverse(st.begin(), st.end());
    reverse(ar, ar + n);
    reverse(st.begin(), st.end());


    return 0;
}