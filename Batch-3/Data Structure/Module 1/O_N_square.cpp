#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    //  for(int i=1;i<=n;i=i+6) //O(N*N)
    // {
    //      for(int j=i;j<=n;j=j+3) 
    //     {
    //         cout<<"Hello"<<endl;
    //     }

    // }
    
    // O(log N) -> 10^18 O(sqrt(n))  -> 10^14
    // O(N) -> 10^7  O(N^2) or O(N*M)-> 10^3
    // O(N log(N))

    for(int i=0;i<=n-1;i=i++) //O(N*N)
    {
         for(int j=1+1;j<n;j++)
        {
            
        }
    }

    return 0;
}